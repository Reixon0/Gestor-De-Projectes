{% extends 'base.html.twig' %}

{% block body %}
    <h1>Estat</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ estat.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ estat.nom }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('estat_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('estat_edit', { 'id': estat.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
